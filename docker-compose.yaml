version: "3.9"

services:
  minio:
    # No Support GUI Replication
    #image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    #image: minio/minio:RELEASE.2024-12-18T13-15-44Z-cpuv1
    # Support GUI replication
    #image: minio/minio:RELEASE.2024-02-17T01-15-57Z
    image: minio/minio:RELEASE.2024-02-24T17-11-14Z
    container_name: minio
    # Host network: container shares the host's network namespace
    network_mode: host
    restart: unless-stopped
    environment:
      # Set your own strong creds (or use an .env file)
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin123!}
      # Optional: tune memory if needed
      # MINIO_CACHE: "on"
    # Bind mount your host data directory to /data in the container
    volumes:
      - ./minio-data:/data
    command: >
      server /data
      --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:9000/minio/health/live"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s
